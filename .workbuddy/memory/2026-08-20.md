# 2026-08-20 工作日志

## PanHub.shenzjd.com 分析 + 复用

用户要求分析 GitHub 项目 https://github.com/wu529778790/panhub.shenzjd.com，识别可复用到"大神搜"的内容。

### 决策
- **复用**：quark4k（夸克论坛搜索）、yunso（云搜聚合）、u3c3（磁力）三个插件移植为 SearchSource
- **跳过**：torrentgalaxy、x1337x 等纯磁力源（用户明确说不要）
- **对齐**：Telegram 抓取方式（已实现类似逻辑，无需改动）
- **缓存分级**：LinkChecker 已有同等 TTL 分级（ok 24h / bad 6h / locked 12h），与 PanHub 一致

### 新增文件
- `service/source/Quark4kSource.kt` — Flarum API，解析夸克论坛帖子中的网盘链接+提取码
- `service/source/YunsoSource.kt` — POST API，解析 HTML 中 `<a url="..." pa="密码">` 标签
- `service/source/U3c3Source.kt` — GET HTML，解析 `<table class="torrent-list">` 磁力列表（默认禁用）

### 修改文件
- `util/UrlKinds.kt` — 新增 `unescapeJsonUrl()`：还原 JSON 转义 URL（`\/` → `/`）
- `service/SearchService.kt` — 注册三个新源（quark4k/yunso 启用，u3c3 默认关闭）；添加 sourceWeight
- `service/source/PanSouSource.kt` — 对 `optString("url")` 结果加 unescapeJsonUrl 处理
- `service/source/WanzhanApiSource.kt` — 同上

### Telegram 对比结论
PanHub 的 TG 抓取（`tg.ts`）与大神搜 `TelegramChannelSource.kt` 核心逻辑一致：
- 直连 t.me vs r.jina.ai 镜像双路探测，保留胜者用于分页
- `?before=` 分页，r.jina.ai 无稳定游标时提前终止
- URL 解析含 r.jina.ai 反向代理还原 + t.me 分享链接展开
- 差异点：PanHub 用 cheerio（服务端），大神搜用 Jsoup（客户端），功能等价

### LinkCheck 缓存对比
| 状态 | PanHub | 大神搜 | 一致 |
|------|--------|--------|------|
| OK   | 24h    | 24h    | ✅ |
| BAD  | 6h     | 6h     | ✅ |
| LOCKED（需密码） | 12h | 12h | ✅ |
| UNCERTAIN | 30m | 30m | ✅ |

### 编译安装
- 新增三个 Source 导致编译错误：
  1. Quark4kSource: `JSON_MEDIA` 用了 `const val` 但类型是 `MediaType`，改为 `val`
  2. Quark4kSource: `postId` nullable 类型用于 `isNotEmpty()` 需加 `?: ""`
  3. YunsoSource: `forEach` lambda 内不能用 `continue/break`，改为 `while + iterator`
  4. U3c3Source: Jsoup `filter` 闭包参数语法问题
- 修复后编译成功，APK 安装到设备 89a707f3 (MT2110)

### 编译警告清理
- YunsoSource.kt 移除 match.groupValues[2] ?: "" 多余 elvis
- 其余三个警告均为已有代码的历史遗留（ProgressDownloader/Api52Source/NetDiskUtils），不改

### 可复用知识沉淀
- TypeScript→Kotlin 移植常见坑已记录到 MEMORY.md
- SearchSource 注册三步模板已记录
- 后续新增源按此模板走

### 技能沉淀
- 创建用户级 Skill （位于 ~/.workbuddy/skills/）
- 内容：TS→Android SearchSource 移植步骤、常见坑对照表、注册模板、URL转义处理、编译环境命令、测试要点
- 与 android-build-install 同级存放

## 项目完整自诊断（14:49）
用户要求对 dashensou 做完整自我诊断并出报告 → `docs/diagnostic_report_2026-08-20.md`。

### 关键结论（实测 assembleDebug + lintDebug）
- 编译通过（含 3 个未提交新源）；Lint 117 项（1 Error + 116 Warning）。
- 🔴 P0 必须修：
  1. `values-night/themes.xml:5` 夜间主题用 `windowLightNavigationBar`（需 API27）但 minSdk=26，Android 8.0 深色模式会崩。日间主题已用 `tools:targetApi="o_mr1"` 守卫，夜间漏了。
  2. 仓库卫生：`.gradle/` 缓存（17文件）与根 `app-debug.apk`（8.7MB，8/18 过时）被 git 跟踪，需 `git rm --cached` + 补 .gitignore。
  3. `U3c3Source` 域名 `u3c3u3c3.u3c3u3c3.u3c3.com` 形态异常，疑似占位/笔误，需核实。
- 🔴 文档滞后：README 写"11 个源/10 列表"，实际 17 实例/16 ID/默认启用 12；README 更新地址用 `main` 但代码/远程是 `master`；`_release_notes.txt` 写 minSdk 24（实际 26）；`update.json` 内 versionCode=1 但 apkUrl 指向 v1.0.1。
- 🟡 安全：usesCleartextTraffic 全开 + JS WebView + allowBackup=true；`network_security_config` 应改按域名放行明文。
- 🟡 依赖普遍落后（19 项 GradleDependency）；compileSdk/targetSdk 34。
- ⚠️ gradle.properties 把 android.user.home 指向另一项目 TalkChatBot/.android，且 daemon/cache 全关。

### 编译坑（本机）
- `./gradlew` 在 Git Bash 下因 `ulimit -H -n` 返回 "maximum" 中断；改用全局 gradle 8.5 的 `gradle.bat` 直接构建（JAVA_HOME 用 Windows 路径格式 C:/... 而非 /c/...）。
